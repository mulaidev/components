{
  "name": "@mulaidev/components",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "start": "astro dev",
    "build": "NODE_ENV=production astro build",
    "css": "npx tailwindcss -o dist/global.css --minify",
    "watch": "npx tailwindcss -o public/global.css --watch"
  },
  "devDependencies": {
    "astro": "^0.19.0-next.2",
    "tailwindcss": "^2.2.7"
  }
}
